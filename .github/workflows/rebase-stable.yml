name: Rebase Stable Branch (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deprecation
        run: |
          echo "overseerr-anime is deprecated and no automatic upstream rebases are planned."
          echo "Migrate to Seerr: https://github.com/seerr-team/seerr"
          exit 1
